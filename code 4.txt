/to check whether it is an armstron number or not
#include<iostream.h>
#include<conio.h>
void main()
{
clrscr();
int num,rem,num1,sum=0,num2;
cout<<"enter a number";
cin>>num;
num2=num;
while(num!=0)
{
  rem=num%10;
  num=num/10;
  num1=rem*rem*rem;
  sum=sum+num1;
  }
  if(sum==num2)
  {
   cout<<"it is a armstrong number";
   }
   else
   {
   cout<<" it is not a armstrong number";
   }
   getch();
   }
